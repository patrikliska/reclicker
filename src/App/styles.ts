import { SxProps, Theme } from '@mui/material';

export const appContainerSx: SxProps<Theme> = {
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
  position: 'relative',
  textAlign: 'center',
};
